# ── API ───────────────────────────────────────────────────────────────────────
# REQUIRED — the app will throw on startup if this is missing.
# Point to the running colombier-api server (no trailing slash, no /api suffix).
#
#   Dev:  http://localhost:8001
#   Prod: https://api.colombier.example.com
#
VITE_API_URL=http://localhost:8001
